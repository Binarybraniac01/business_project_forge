# make sure to copy this file to .env ( create it if it doesn't exist) and fill in the values

# Environment Variables
# Supabase Configuration
SUPABASE_URL=
SUPABASE_KEY=
SUPABASE_SERVICE_KEY=

# Flask Configuration
FLASK_SECRET_KEY=
FLASK_ENV=
FLASK_DEBUG=

# Admin Panel Credentials (for basic auth)
ADMIN_USERNAME=
ADMIN_PASSWORD=

# CORS Settings
CORS_ORIGINS=

# Email Notification Settings (Gmail SMTP)
SMTP_HOST=
SMTP_PORT=
SMTP_EMAIL=
SMTP_PASSWORD=
NOTIFICATION_EMAIL=
